class NotificationCenterView{constructor(app){this.app=app}async render(){const n=await this.app.api.get('/notifications');document.getElementById('view').innerHTML=`<div class='card'><h3>Notifications</h3>${(n.notifications||[]).map(x=>`<div class='card'>${UIComponents.badge(x.type,'degraded')} ${x.message} <small>${Utils.timeAgo(x.timestamp)}</small></div>`).join('')}</div>`}};window.NotificationCenterView=NotificationCenterView;
