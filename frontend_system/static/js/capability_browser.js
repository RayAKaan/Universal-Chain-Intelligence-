class CapabilityBrowserView{constructor(app){this.app=app}async render(){const d=await this.app.api.getCapabilities({});const caps=d.capabilities||[];document.getElementById('view').innerHTML=`<div class='card'><h3>Capabilities</h3>${UIComponents.searchBar('Search capabilities')}</div><div class='grid grid-2'>${caps.map(c=>`<div class='card'><h4>${c.name}</h4>${UIComponents.badge(c.type,'healthy')} ${UIComponents.badge(c.health,c.health)}<div>Latency: ${c.latency_ms}ms</div><div>Reliability: ${(c.reliability*100).toFixed(1)}%</div><button class='btn' data-id='${c.id}'>Details</button></div>`).join('')}</div>`;document.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>this.viewCapabilityDetail(b.dataset.id))}async viewCapabilityDetail(id){const d=await this.app.api.getCapability(id);UIComponents.modal('Capability Detail',UIComponents.codeBlock(JSON.stringify(d,null,2)))}};window.CapabilityBrowserView=CapabilityBrowserView;
